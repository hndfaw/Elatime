# Elatime environment variables (all optional).
#
# The site works with no env vars. Set this in production so OpenGraph/Twitter
# link-preview image URLs resolve to your real domain (otherwise a default
# placeholder is used). On Vercel: Project → Settings → Environment Variables.
NEXT_PUBLIC_SITE_URL=https://your-elatime-domain.vercel.app
